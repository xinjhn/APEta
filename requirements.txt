# requirements.txt (catat versi -> PANDUAN Bagian 3)
fastapi==0.137.2
httpx==0.28.1  # dipakai scratch/concurrency_liveness.py (AsyncClient langsung)
httpx2==2.4.0  # dipakai starlette.testclient (tests/test_parity.py) -- starlette==1.3.1
               # mendeprekasi httpx untuk TestClient; tanpa ini test_parity.py
               # masih JALAN tapi mencetak StarletteDeprecationWarning per impor.
psutil==7.2.2
starlette==1.3.1
strawberry-graphql==0.317.2
uvicorn==0.49.0

# --- Dependensi analisis (tools/analyze_factorial.py, combine_factorial_results.py) ---
# Tidak dipakai server -- hanya skrip post-hoc statistik/plot. Sebelumnya
# diinstal ad-hoc tanpa pin (tidak di requirements.txt), jadi tak reproducible.
matplotlib==3.10.9
numpy==2.5.0
pandas==3.0.3
scipy==1.17.1
seaborn==0.13.2
statsmodels==0.14.6
